const notes = ['do', 're', 'mi', 'fa', 'sol', 'la', 'si']
for (i = notes.length-1; i >= 0; i--) { // i = notes.length-1
    console.log(notes[i])
}

for (let i = 0; i < array.length; i++) {
    const element = array[i];
    
}

